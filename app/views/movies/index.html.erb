<% if !user_signed_in? %>
  <div class="jumbotron">
    <h1>Your Favorite Movies Reviewed by You!</h1>
    <p> Who needs Kenneth Turan or Justin Chang when you can write your own reviews? Actually, those guys are pretty great and their reviews are pretty awesome.</p>
    <p>Read their reviews, but also write your own here!</p>
    <p> <%= link_to 'Sign Up To Write A Review', new_user_registration_path, class: 'btn btn-primary btn-lg' %></p>
  </div>
<% end %>

<div class="row">
  <% @movies.each do |movie| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag movie.image.url(:medium), class: 'image'), movie %>
      </div>
    </div>
  <% end %>
</div>
